[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fastapi[all]",
    "langchain-openai",
    "psycopg2-binary",
    "pydantic-settings",
    "python-dotenv",
    "sqlalchemy",
    "uvicorn",
]
